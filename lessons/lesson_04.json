{
  "title": "Bubble Sort",
  "description": "A simple sorting algorithm using nested loops.",
  "code": "public void bubbleSort(int[] arr) {\\n    int n = arr.length;\\n    for (int i = 0; i < n - 1; i++) {\\n        for (int j = 0; j < n - i - 1; j++) {\\n            if (arr[j] > arr[j + 1]) {\\n                int temp = arr[j];\\n                arr[j] = arr[j + 1];\\n                arr[j + 1] = temp;\\n            }\\n        }\\n    }\\n}",
  "explanation": "Repeatedly walks through the array, comparing adjacent elements. If they are in the wrong order, it swaps them. The largest values bubble to the end."
}